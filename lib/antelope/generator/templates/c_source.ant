#include "{{= file }}.h"

{{= namespace }}_token_string(enum {{= namespace }}_token token)
{
  switch(token)
  {
{{ grammar.terminals.each do |terminal| }}
  case {{= guard_name }}_{{= terminal.name }}:
    return {{= terminal.to_s.inspect }};
{{ end }}
  }
}
